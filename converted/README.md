# Converted Models

Intermediate converted model artifacts may be written here during local runs.

These generated files are intentionally ignored because they can be recreated
from the source model and configuration.
